# the "Partners" tab's own photos

Same convention as `src/team/`: drop a `.webp` / `.jpg` / `.png` / `.avif` in
here named after the person in `src/data/partners.js`, matched by name, not
by position. An empty slot falls back to a plain initials avatar.
