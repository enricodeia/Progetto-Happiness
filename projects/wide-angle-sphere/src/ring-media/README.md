# shared fallback for both rings

A single folder both rings fall back to when their own is empty
(`src/people/` for ring A, `src/experiences/` for ring B). Useful while there is
only one set of images to try.

Files are used in filename order, so a numeric prefix controls where an image
sits on the orbit. Square crops read cleanest.
